const Gameboard = (() => { //module
    const moves = ["X", "O", "X", "O", "X", "O", "X", "O", "X"];
    return {displayController(moves)};
})();

const playerFactory = (name, symbol) => { //factory
    return {name, symbol};
}

const playGame = (() => { //module

})();

const displayController = ((moves) => { //module
    
})();

